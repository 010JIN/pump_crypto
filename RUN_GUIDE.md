# ?? ??????

## Step 0: ??????

### ????

```bash
# ?? Python ?? (?? 3.9+)
python --version
# ?
python3 --version

# ?? PostgreSQL (?? 14+)
psql --version

# ?? Redis (?? 7+)
redis-server --version
```

????????????

**Ubuntu/Debian:**
```bash
# Python 3.9+
sudo apt update
sudo apt install python3.9 python3.9-venv python3-pip

# PostgreSQL
sudo apt install postgresql postgresql-contrib

# Redis
sudo apt install redis-server
```

**macOS:**
```bash
# ?? Homebrew
brew install python@3.9
brew install postgresql
brew install redis
```

**Windows:**
- Python: ? https://www.python.org/downloads/ ??
- PostgreSQL: ? https://www.postgresql.org/download/windows/ ??
- Redis: ?? WSL ?? https://github.com/microsoftarchive/redis/releases

---

## Step 1: ????

```bash
# ??????
cd /workspace

# ??????
ls -la
# ????: src/, config.yaml, requirements.txt, main.py ?
```

---

## Step 2: ??????

```bash
# ??????
python3 -m venv venv

# ??????
# Linux/Mac:
source venv/bin/activate

# Windows:
# venv\Scripts\activate

# ?????? (???????? (venv) ??)
which python  # ???? venv ??
```

---

## Step 3: ????

```bash
# ?? pip
pip install --upgrade pip

# ????
pip install -r requirements.txt
```

**?? ???? - TA-Lib ??**

?? `ta-lib` ?????????????? C ?????????

**?? 1: ?? TA-Lib (????????)**
```bash
# ?? requirements.txt???? ta-lib ??
sed -i 's/^ta-lib/#ta-lib/' requirements.txt

# ????
pip install -r requirements.txt
```

**?? 2: ???? TA-Lib**
```bash
# Ubuntu/Debian
wget http://prdownloads.sourceforge.net/ta-lib/ta-lib-0.4.0-src.tar.gz
tar -xzf ta-lib-0.4.0-src.tar.gz
cd ta-lib/
./configure --prefix=/usr
make
sudo make install
cd ..
pip install ta-lib

# macOS
brew install ta-lib
pip install ta-lib
```

---

## Step 4: ???????

### 4.1 ?? PostgreSQL

```bash
# Ubuntu/Debian
sudo systemctl start postgresql
sudo systemctl status postgresql  # ????

# macOS
brew services start postgresql

# Windows
# ????????? PostgreSQL ??
```

### 4.2 ?????

```bash
# ??? postgres ?? (Linux)
sudo -u postgres psql

# ????? (macOS/Windows)
psql -U postgres
```

? PostgreSQL ?????
```sql
-- ?????
CREATE DATABASE crypto_pump_dump;

-- ??
\q
```

### 4.3 ?? Redis

```bash
# ???? Redis
redis-server --daemonize yes

# ?? Redis ????
redis-cli ping
# ????: PONG
```

---

## Step 5: ??????

```bash
# ??????
cp .env.example .env

# ??????
nano .env  # ??????????
```

**???? .env ??:**
```env
# PostgreSQL ??
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=crypto_pump_dump
POSTGRES_USER=postgres
POSTGRES_PASSWORD=????

# Redis ??
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Binance API (?????REST API)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Telegram (??)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# ????
LOG_LEVEL=INFO
```

**?????** (nano: Ctrl+X, ?? Y, ?? Enter)

---

## Step 6: ??????

```bash
# ??????????
python scripts/setup_database.py
```

**????:**
```
?? Setting up database...
==================================================
? Connected to PostgreSQL: localhost:5432/crypto_pump_dump
? Database tables created successfully
? Connected to Redis: localhost:6379

? Database setup completed successfully!

PostgreSQL Tables Created:
  ? klines
  ? tickers
  ? trades_aggregated
  ? orderbook_snapshots
  ? factor_values
  ? detection_events

? Redis connection verified
```

???????????
1. PostgreSQL ??????
2. Redis ??????
3. .env ??????????

---

## Step 7: ???? (?????)

```bash
# ??????
python scripts/test_system.py
```

**????:**
```
==================================================
?? Running System Tests
==================================================

1??  Testing Database Connections...
   ? Database connections OK

2??  Testing Factor Calculations...
   ? Factor calculations OK (xx factors)

3??  Testing Detection Engine...
   ? Detection engine OK
      PUMP confidence: xx.xx%
      DUMP confidence: xx.xx%

4??  Testing Alert System...
   ? Alert system OK

==================================================
?? Test Summary
==================================================

Passed: 4/4

? All tests passed!
```

---

## Step 8: ?? ?????

```bash
# ????
python main.py
```

**????:**
```
======================================================================
?? Crypto Pump & Dump Detection System - MVP
======================================================================
2025-11-03 10:30:00 | INFO     | ?? Initializing Crypto Pump & Dump Detection System...
2025-11-03 10:30:01 | INFO     | ?? Connecting to databases...
2025-11-03 10:30:01 | INFO     | ? Connected to PostgreSQL: localhost:5432/crypto_pump_dump
2025-11-03 10:30:01 | INFO     | ? Connected to Redis: localhost:6379
2025-11-03 10:30:01 | INFO     | ? System initialized successfully
2025-11-03 10:30:02 | INFO     | ?? Filtering coins for monitoring...
2025-11-03 10:30:05 | INFO     | ? Filtered to 50 coins for monitoring
2025-11-03 10:30:05 | INFO     | ?? Monitoring 50 symbols
2025-11-03 10:30:05 | INFO     | ?? Starting data collection...
2025-11-03 10:30:06 | INFO     | ?? Starting BinanceCollector...
2025-11-03 10:30:07 | INFO     | ? BinanceCollector started successfully
2025-11-03 10:30:07 | INFO     | ?? Starting detection loop...
2025-11-03 10:30:07 | INFO     | ?? Detection loop started

# ???????????...
```

---

## Step 9: ??????

### ??????

??**??????**????

```bash
# ?????
tail -f logs/crypto_monitor_*.log

# ???????
tail -f logs/*.log
```

### ??????

```bash
# ????????
bash scripts/monitor_system.sh
```

---

## Step 10: ??????

?????????

**????:**
1. **?1-2??**: ???????????
2. **2-5???**: ??????
3. **5???**: ???? PUMP/DUMP

**????????????:**

```
======================================================================
?? PUMP DETECTED! ??
======================================================================
Symbol:      SOLUSDT
Price:       $125.4500
Confidence:  87.50%
Risk Level:  CRITICAL
Time:        2025-11-03 10:35:22

Trigger Factors:
  ? price_velocity_5m: 15.20 (score: 0.92)
  ? volume_spike_score: 0.88 (score: 0.88)
  ? buy_sell_ratio: 3.50 (score: 0.85)
======================================================================
```

---

## Step 11: ????

?????????

```bash
# ? Ctrl+C

# ????????
?? Stopping system...
Stopping BinanceCollector...
BinanceCollector stopped
PostgreSQL connection closed
Redis connection closed
System stopped
?? Shutting down gracefully...
```

---

## ?? ????

### ?? 1: PostgreSQL ????

```bash
# ?? PostgreSQL ????
sudo systemctl status postgresql

# ?????????
psql -U postgres -l | grep crypto_pump_dump

# ????
psql -U postgres -d crypto_pump_dump -c "SELECT 1"
```

### ?? 2: Redis ????

```bash
# ?? Redis ????
redis-cli ping

# ??????????
redis-server --daemonize yes
```

### ?? 3: ?????????

??**???**????
1. Pump & Dump ?????????
2. ???????? (??5??)
3. ?????? (confidence_threshold: 0.65)

**???? - ????????:**

?? `config.yaml`:
```yaml
detection:
  pump:
    confidence_threshold: 0.50  # ? 0.65 ??? 0.50
  dump:
    confidence_threshold: 0.50
```

???????

### ?? 4: ??????

```bash
# ?????????
source venv/bin/activate

# ??????
pip install -r requirements.txt

# ?? Python ??
python -c "import sys; print(sys.path)"
```

### ?? 5: Binance API ??

???? Binance ?????
1. ??????
2. ??? IP ?????? VPN?
3. WebSocket ???????????

---

## ?? ????????

### ?? 1: ?????

```bash
psql -U postgres crypto_pump_dump
```

```sql
-- ???????
SELECT COUNT(*) FROM klines;
SELECT COUNT(*) FROM tickers;
SELECT COUNT(*) FROM factor_values;

-- ?????????
SELECT symbol, timestamp, price_velocity_5m, volume_spike_score
FROM factor_values
ORDER BY timestamp DESC
LIMIT 10;

-- ??????
SELECT * FROM detection_events ORDER BY detection_time DESC LIMIT 5;
```

### ?? 2: ?? Redis

```bash
redis-cli
```

```redis
# ???? key
KEYS *

# ??????
ZRANGE watchlist 0 10

# ?????????
GET price:BTCUSDT

# ??
exit
```

### ?? 3: ????

```bash
# ???????
tail -n 50 logs/crypto_monitor_*.log

# ??????
grep "PUMP DETECTED" logs/crypto_monitor_*.log
grep "ERROR" logs/crypto_monitor_*.log
```

---

## ?? ??????

?????????????

```bash
# ???????
python -c "from src.storage import init_databases; init_databases()"

# ??????
python src/factors/price_factors.py

# ?? PUMP ???
python src/detectors/pump_detector.py

# ??????
python src/alerts/console_alert.py

# ??????
python scripts/test_system.py
```

---

## ?? ?? Telegram ?? (??)

1. **?? Telegram Bot:**
   - ?? Telegram??? @BotFather
   - ?? `/newbot`
   - ???????? Bot Token

2. **???? Chat ID:**
   ```bash
   # ???? bot ??????
   # ????:
   curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   # ???? JSON ??? chat.id
   ```

3. **?? .env:**
   ```env
   TELEGRAM_BOT_TOKEN=??_bot_token
   TELEGRAM_CHAT_ID=??_chat_id
   ```

4. **?? Telegram:**
   ?? `config.yaml`:
   ```yaml
   alerts:
     telegram:
       enabled: true
   ```

---

## ?? ??????

?????????

### ?? systemd (Linux)

?? `/etc/systemd/system/crypto-detector.service`:
```ini
[Unit]
Description=Crypto Pump & Dump Detector
After=network.target postgresql.service redis.service

[Service]
Type=simple
User=?????
WorkingDirectory=/workspace
Environment="PATH=/workspace/venv/bin"
ExecStart=/workspace/venv/bin/python /workspace/main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

????:
```bash
sudo systemctl daemon-reload
sudo systemctl start crypto-detector
sudo systemctl enable crypto-detector  # ????
sudo systemctl status crypto-detector  # ????
```

### ?? Docker (??)

?? `Dockerfile`:
```dockerfile
FROM python:3.9-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .

CMD ["python", "main.py"]
```

---

## ?? ?????

???????
1. ?? `logs/crypto_monitor_*.log` ????
2. ?? `python scripts/test_system.py` ????
3. ?? `.env` ??????
4. ?? PostgreSQL ? Redis ????

---

**???????** ??

?????????????
