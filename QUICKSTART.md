# ?? ??????

## 5??????

### ????

?????:
- Python 3.9+
- PostgreSQL 14+
- Redis 7+

### ?? 1: ????

```bash
# ??????
python -m venv venv
source venv/bin/activate  # Linux/Mac

# ????
pip install -r requirements.txt
```

### ?? 2: ?????

```bash
# ?? PostgreSQL (???????)
# Linux: sudo systemctl start postgresql
# Mac: brew services start postgresql
# Windows: ?????????

# ?? Redis
redis-server &

# ??Redis
redis-cli ping  # ??? PONG
```

### ?? 3: ??????

```bash
# ??????
cp .env.example .env

# ?? .env ??
nano .env  # ??????????
```

**????** (???????):
```env
POSTGRES_PASSWORD=your_password
```

### ?? 4: ??????

```bash
python scripts/setup_database.py
```

?? `? Database setup completed successfully!` ?????

### ?? 5: ????

```bash
python scripts/test_system.py
```

?????????

### ?? 6: ????

```bash
python main.py
```

**????**???????????1-2????????

---

## ????

### Q: PostgreSQL????

**A**: ??PostgreSQL??????? `.env` ????????
```bash
psql -U postgres -c "SELECT 1"
```

### Q: Redis????

**A**: ??Redis????
```bash
redis-cli ping
```

### Q: ?????????

**A**: ??????Pump & Dump?????????????:
1. ?? `config.yaml` ?? `confidence_threshold` (???0.65??0.5)
2. ???? `tail -f logs/crypto_monitor_*.log`

### Q: ??????

**A**: ????????
```yaml
# config.yaml
data_collection:
  coin_filters:
    min_24h_volume: 5000000  # ????
```

### Q: ????????

**A**: ??????
```bash
# ??????
python src/factors/price_factors.py
python src/detectors/pump_detector.py

# ??????
python scripts/test_system.py
```

---

## ????

### ???????

?? `config.yaml`:

```yaml
detection:
  pump:
    confidence_threshold: 0.65  # ?? = ???, ????
                                # ?? = ???, ????
```

### ??Telegram??

1. ??Telegram Bot (?? @BotFather)
2. ??Bot Token
3. ????Chat ID
4. ?? `.env`:
```env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```
5. ??Telegram:
```yaml
# config.yaml
alerts:
  telegram:
    enabled: true
```

### ??????

?? `main.py` ??????:
```python
# ????????????
self.monitored_symbols = ['BTCUSDT', 'ETHUSDT', 'BNBUSDT']
```

---

## ????

### ?????

```sql
-- ????????
CREATE INDEX CONCURRENTLY idx_factors_recent 
ON factor_values(symbol, timestamp DESC) 
WHERE timestamp > NOW() - INTERVAL '1 day';
```

### Redis??

?? `redis.conf`:
```conf
maxmemory 2gb
maxmemory-policy allkeys-lru
```

### ????

????:
- **??**: 2 CPU, 4GB RAM
- **??**: 4 CPU, 8GB RAM
- **???**: 8+ CPU, 16GB RAM

---

## ??????

### ??????

```bash
# ???
tail -f logs/crypto_monitor_*.log

# ????
tail -f logs/detection.log

# ????
tail -f logs/factors.log
```

### ??????

??Python REPL:
```python
from src.storage import init_databases
from src.detectors import DetectionEngine

pg, redis = init_databases()
engine = DetectionEngine(postgres_manager=pg, redis_manager=redis)

# ??????
events = engine.get_active_events()
print(f"Active events: {len(events)}")
for symbol, event in events.items():
    print(f"{symbol}: {event['event_type']} - {event['confidence']:.2%}")
```

### ???????

```bash
psql -U postgres crypto_pump_dump -c "
SELECT 
  'klines' as table_name, COUNT(*) as rows FROM klines
UNION ALL
SELECT 'detection_events', COUNT(*) FROM detection_events
UNION ALL
SELECT 'factor_values', COUNT(*) FROM factor_values;
"
```

---

## ????

### ????

```bash
# ? Ctrl+C
# ??????????????
```

### ????

```bash
# ????
ps aux | grep main.py

# ????
kill -9 <PID>
```

---

## ???

1. ? ??????????1-2?????
2. ?? ???????????
3. ?? ???????????
4. ?? ???????? (Phase 2)

---

## ????

- ?? ????: [README.md](./README.md)
- ??? ????: [ARCHITECTURE.md](./ARCHITECTURE.md)
- ?? ????: GitHub Issues
- ?? ????: GitHub Discussions

**Good luck! ??**
